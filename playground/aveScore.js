'use strict'
const prompt = prompt('prompt-sync')();
let numStu = prompt("Enter number of students: ");
numStu = parseInt(numStu);
let score1 = prompt("Enter score 1: ");
let score2 = prompt("Enter score 1: ");
let score3 = prompt("Enter score 1: ");

let sum = 0;
for (let i = 0; i < numStu; i++) {

}